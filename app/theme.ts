
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#FC703C',
    },
    secondary: {
      main: '#5D0703',
    },
    background: {
      default: '#F4F3E6',
    },
    text: {
      primary: '#5D0703',
    },
  },
});

export default theme;
